<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.seraph.mvprxjavaretrofit">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icon_placeholder"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.module.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--图片预览 android:process=":picture" -->
        <activity
            android:name=".ui.module.common.photopreview.PhotoPreviewActivity"
            android:theme="@style/AppTheme.NoStatusBar" />
        <!--权限设置-->
        <activity android:name=".ui.module.common.permission.PermissionsActivity" />
        <!--Design布局测试-->
        <activity
            android:name=".ui.module.test.DesignLayoutTestActivity"
            android:theme="@style/AppTheme.NoStatusBar" />


    </application>

</manifest>